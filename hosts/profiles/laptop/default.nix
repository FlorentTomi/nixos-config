{ ... }:
{
  # Laptop archetype scaffold — mirrors hosts/profiles/gaming-desktop/'s
  # shape. Left empty until a real laptop host exists to validate actual
  # requirements against (power-management, wifi powersave = true, etc.)
  # rather than guessing ahead of the hardware.
  imports = [ ];
}
